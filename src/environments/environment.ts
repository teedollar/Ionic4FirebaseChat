// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAigQRZDEVYK-4xTaUWZdh-zg0dK2wAXDo",
    authDomain: "ionic4firebasechat.firebaseapp.com",
    databaseURL: "https://ionic4firebasechat.firebaseio.com",
    projectId: "ionic4firebasechat",
    storageBucket: "ionic4firebasechat.appspot.com",
    messagingSenderId: "549328878950"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
